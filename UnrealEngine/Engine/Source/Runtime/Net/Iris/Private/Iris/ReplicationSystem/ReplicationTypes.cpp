// Copyright Epic Games, Inc. All Rights Reserved.

// =====================================================================================================================
// ReplicationTypes.cpp —— 与对应 .h 配对的"占位"翻译单元
// ---------------------------------------------------------------------------------------------------------------------
// ReplicationTypes.h 全部内容均为 POD 结构体 / 枚举 / typedef，不需要任何函数定义；
// 但仍保留该 .cpp 以保证 .h 至少被编译一次（防止只在某些配置下被引用导致语法错误漏检），
// 同时为未来添加独立类型的实现保留入口。
// =====================================================================================================================

#include "Iris/ReplicationSystem/ReplicationTypes.h"

